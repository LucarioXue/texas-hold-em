# 产品名称：德州扑克高阶决策辅助 Web App (MVP)

## 1. 产品概述
这是一个专为线下德州扑克玩家设计的移动端 Web 应用。核心价值在于**“极速录入 + 零延迟本地计算 + 情绪价值决策辅助”**。产品纯前端运行（无需后端），保证线下实战时的极简与快速。

## 2. 核心功能模块与用户流 (User Flow)

### 2.1 全局控制条：玩家人数滑动器
- **位置：** 页面顶部或显著位置。
- **功能：** 用户可滑动选择当前牌局剩余玩家数（2-10人）。
- **作用：** 该数值将实时作为参数传入底层的蒙特卡洛算法引擎，动态影响胜率计算结果。

### 2.2 极速录入模块：双列专属扑克键盘
- **痛点解决：** 摒弃传统的 52 张全屏选牌，采用分离式高效键盘。
- **UI 布局：** 屏幕下方固定区域。左侧一列为 4 个巨大的花色按钮（♠️♥️♣️♦️），右侧为点数矩阵（2-A）。
- **交互：** 左手选花色，右手选点数，选中后卡片自动填入当前激活的空位（底牌或公共牌），并自动 focus 下一个空位。支持一键撤销/清空。

### 2.3 状态 1：翻牌前 (Pre-Flop) - 初始胜率
- **输入：** 录入 2 张底牌。
- **输出：** 核心展示区域显示“基础胜率”(Win Rate)。
- **决策辅助：** 基于底牌质量和剩余人数，给予简短的文本提示（例：“AA - 绝对优势，建议强力加注” 或 “72o - 极其危险，建议弃牌”）。

### 2.4 状态 2：翻牌圈及之后 (Flop/Turn/River) - 深度分析
- **输入：** 录入 3-5 张公共牌。
- **输出（核心亮点）：**
  1. **胜率/平局率：** 实时更新。
  2. **大白话听牌分析 (Outs)：** 例如“两头抽顺，有 8 张补牌，下一轮成牌率约 32%”。
  3. **牌型分布概率图：** 使用极简的水平进度条或列表，展示用户最终组合成高牌、对子、顺子、同花等各牌型的概率百分比。

## 3. 技术约束与实现思路建议
1. **纯本地计算：** 为了保证 0 延迟，绝不调用外部 API。所有的概率推演请使用 JavaScript 纯前端实现。
2. **算法逻辑：**
   - 采用穷举法（当未发牌较少时）或蒙特卡洛模拟算法 (Monte Carlo Simulation) 跑 5000-10000 次随机发牌，来估算胜率和分布。
   - 需实现一个轻量级的扑克牌大小比较算法器（Evaluator）。
3. **移动端适配：** 采用 Mobile-first 设计，禁止横向滚动，确保在手机浏览器 Safari/Chrome 中打开时体验类似于原生 App。禁止长按选中文本。

---

## 4. V1.1：Pot Odds + EV 决策引擎

### 4.1 设计理念
从 "Calculator（概率计算器）" 升级为 "Decision Assistant（决策助手）"。线下的真实场景中，用户只在剩 3-4 人、有人下大注时才会估算底池计算 EV。平时懒得记底池就看胜率。因此采用**"即时快照"**模式——用户估算数字点筹码按钮，实时得到 EV 结论；不输入时自动降级为纯胜率显示。

### 4.2 翻牌前胜率修正 (V1.1.1)
翻牌前原先使用查表法（heads-up equity × 指数外推），在 6+ 人局严重失真（23s 从 ~10% 错到 0.6%）。现已改为与翻牌后相同的蒙特卡洛模拟引擎（4000 次随机发公共牌 + 对手手牌），翻牌前胜率精度与翻牌后一致。

### 4.2 筹码快照输入
- **底池 (Pot) 与 跟注额 (Bet)**：各一行筹码面额按钮 `[5] [10] [20] [50] [100] [500]`，点按累加。
- 每行末尾有独立 ↺ 归零按钮（**禁止使用长按**，避免与 PWA 系统手势冲突）。
- **不做跨街自动继承**——用户录入新公共牌时，Pot/Bet 保持原值不变。每次计算都是独立的"快照"。
- **优雅降级**：Pot=0 且 Bet=0 时，不渲染 Pot Odds / EV 面板，界面等同于原来的纯胜率计算器。

### 4.3 赔率与 EV 算法
- **需要胜率** = Call / (Pot + Call + Call)
- **期望价值** = Win% × Pot − Lose% × Call
- EV > 0 → 绿色 "✓ +EV X.X BB 建议跟注"
- EV < 0 → 红色 "✗ −EV X.X BB 建议弃牌"

### 4.4 信息层级重构
决策面板从上到下依次为：
1. **决策头区**：胜率 + [条件] 赔率对比 + EV 结论（最醒目的位置）
2. **进度条**：胜率可视化，叠加"需要胜率"刻度标记
3. **筹码输入区**：Pot/Bet 两排快捷按钮
4. **翻牌前评论** 或 **详细分析折叠面板**（Outs + 牌型分布，默认折叠，保持界面清爽）
5. **模拟方法标注**