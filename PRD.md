# 产品名称：德州扑克高阶决策辅助 Web App (MVP)

## 1. 产品概述
这是一个专为线下德州扑克玩家设计的移动端 Web 应用。核心价值在于**“极速录入 + 零延迟本地计算 + 情绪价值决策辅助”**。产品纯前端运行（无需后端），保证线下实战时的极简与快速。

## 2. 核心功能模块与用户流 (User Flow)

### 2.1 全局控制条：玩家人数滑动器
- **位置：** 页面顶部或显著位置。
- **功能：** 用户可滑动选择当前牌局剩余玩家数（2-9人）。
- **作用：** 该数值将实时作为参数传入底层的蒙特卡洛算法引擎，动态影响胜率计算结果。

### 2.2 极速录入模块：双列专属扑克键盘
- **痛点解决：** 摒弃传统的 52 张全屏选牌，采用分离式高效键盘。
- **UI 布局：** 屏幕下方固定区域。左侧一列为 4 个巨大的花色按钮（♠️♥️♣️♦️），右侧为点数矩阵（2-A）。
- **交互：** 左手选花色，右手选点数，选中后卡片自动填入当前激活的空位（底牌或公共牌），并自动 focus 下一个空位。支持一键撤销/清空。

### 2.3 状态 1：翻牌前 (Pre-Flop) - 初始胜率
- **输入：** 录入 2 张底牌。
- **输出：** 核心展示区域显示“基础胜率”(Win Rate)。
- **决策辅助：** 基于底牌质量和剩余人数，给予简短的文本提示（例：“AA - 绝对优势，建议强力加注” 或 “72o - 极其危险，建议弃牌”）。

### 2.4 状态 2：翻牌圈及之后 (Flop/Turn/River) - 深度分析
- **输入：** 录入 3-5 张公共牌。
- **输出（核心亮点）：**
  1. **胜率/平局率：** 实时更新。
  2. **大白话听牌分析 (Outs)：** 例如“两头抽顺，有 8 张补牌，下一轮成牌率约 32%”。
  3. **牌型分布概率图：** 使用极简的水平进度条或列表，展示用户最终组合成高牌、对子、顺子、同花等各牌型的概率百分比。

## 3. 技术约束与实现思路建议
1. **纯本地计算：** 为了保证 0 延迟，绝不调用外部 API。所有的概率推演请使用 JavaScript 纯前端实现。
2. **算法逻辑：**
   - 采用穷举法（当未发牌较少时）或蒙特卡洛模拟算法 (Monte Carlo Simulation) 跑 5000-10000 次随机发牌，来估算胜率和分布。
   - 需实现一个轻量级的扑克牌大小比较算法器（Evaluator）。
3. **移动端适配：** 采用 Mobile-first 设计，禁止横向滚动，确保在手机浏览器 Safari/Chrome 中打开时体验类似于原生 App。禁止长按选中文本。